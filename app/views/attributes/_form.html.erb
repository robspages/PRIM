<%= form_for(@attribute) do |f| %>
  <% if @attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attribute.errors.count, "error") %> prohibited this attribute from being saved:</h2>

      <ul>
      <% @attribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :attributeType_id %><br>
    <%= f.number_field :attributeType_id %>
  </div>
  <div class="field">
    <%= f.label :isLocalizable %><br>
    <%= f.check_box :isLocalizable %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
